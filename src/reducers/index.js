function reducer(state, action) {
    switch (action.type) {
        case '' /* ACTION TYPE */: 
            return state /* UPDATED STATE */
        default:
            return state
    }
}

export default reducer